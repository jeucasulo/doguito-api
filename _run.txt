How to run with NODE

```
node appFileNamePath
node bin/www
```

or search for some script in package.json (shortcuts commands comandos de atalho)

```json
  "scripts": {
    "start": "node ./bin/www",

```

then run the NPM found command (START in that case)

```json
npm start
```